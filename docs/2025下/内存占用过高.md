## 1. 找到内存占用最高的10个进程

```bash
ps aux --sort=-%mem | head -10
```

## 2. 杀掉匹配完整命令行的进程

```bash
pkill -f "命令"
```

## 3. 统计 CDN 日志文件中第12列（图片URL）的出现次数

```bash
a=oneLogPath # 日志文件路径

awk '{count[$12]++} END {for (url in count) print count[url], url}' $a | sort -nr | head -n 5
```

## 4. pm2 logs 指定行数

```bash
pm2 logs my-app --lines 50
```

## 5. 部署后网页打不开

ping 不通，telnet、nslookup 都可以，可能是证书有问题

### 5.1. hans

- han （汉） s：simplified
- han （汉） t：tradition

## 6. 游戏

需要从游戏拿的：

- 账号
- 匹配
- 结算
